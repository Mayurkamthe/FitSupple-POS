# Non-sensitive, source-controlled defaults. Sensitive values (WhatsApp tokens, etc.)
# live in the external config/app.properties file created next to the installed app —
# see com.fitsupplepos.config.AppConfig. Do NOT put secrets in this file.
app.name=FitSupple POS
app.version=1.0.0
db.file.name=fitsupple.db
backup.autoBackupOnRestore=true
invoice.thermalWidthMm=80
